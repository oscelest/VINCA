package com.noxyspace.vinca.canvas.symbols.empty;

import android.content.Context;

import com.noxyspace.vinca.canvas.symbols.SymbolContainerCollapsed;

public class SymbolEmpty extends SymbolContainerCollapsed{
    public SymbolEmpty(Context context, int resId) {
        super(context, resId);
    }
}
